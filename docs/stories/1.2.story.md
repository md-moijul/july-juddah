## 1.2: Certificate Generation Form & Preview

**Status:** Done

**Story:**
**As a** participant, **I want** to enter my details and see a live preview of my certificate, **so that** I can verify its accuracy before downloading.

**Acceptance Criteria:**
1.  Clicking the landing page CTA navigates the user to the `/generate` page.
2.  The page contains input fields for "Full Name" and "Location".
3.  A `CertificatePreview` component is created and displayed on the page.
4.  The `CertificatePreview` component uses the official, pre-defined certificate design template.
5.  Any text typed into the "Full Name" and "Location" fields is reflected in the `CertificatePreview` component in real-time.
6.  A "Download PDF" button is visible but is not yet functional.

**Dev Notes:**
*   **Previous Story Insights**: The project is initialized and basic dependencies are set up from Story 1.1. The `apps/web` directory is the root for the frontend application.
*   **Data Models**:
    *   `CertificateData` interface:
        ```typescript
        export interface CertificateData {
          fullName: string;
          location: string;
        }
        ```
        [Source: docs/architecture/data-models.md]
*   **API Specifications**:
    *   The frontend will eventually consume the `POST /api/generate-pdf` endpoint. This story will make the "Download PDF" button visible but not functional. [Source: docs/architecture/components.md#2-pdf-generation-service, docs/architecture/core-workflows.md]
*   **Component Specifications**:
    *   `CertificatePreview` component: This component will display the certificate based on user input. It should use the official certificate design template. [Source: docs/architecture/frontend-architecture.md#component-architecture, docs/architecture/components.md#1-frontend-application]
    *   Input fields for "Full Name" and "Location" will be standard HTML input elements, styled with Tailwind CSS and potentially using `shadcn/ui` components.
*   **File Locations**:
    *   The generator page will be located at `apps/web/src/app/generate/page.tsx`. [Source: docs/architecture/frontend-architecture.md#routing-architecture]
    *   The `CertificatePreview` component will be located at `apps/web/src/components/certificate-preview.tsx`. [Source: docs/architecture/frontend-architecture.md#component-architecture]
*   **Testing Requirements**:
    *   Unit and component tests should be written using Jest and React Testing Library. Tests should be co-located with the code they are testing. [Source: docs/architecture/testing-strategy.md]
    *   Tests should verify that input fields are rendered, the `CertificatePreview` component is displayed, and real-time updates occur.
*   **Technical Constraints**:
    *   **Frontend Framework**: Next.js [Source: docs/architecture/tech-stack.md]
    *   **UI Component Library**: shadcn/ui [Source: docs/architecture/tech-stack.md]
    *   **CSS Framework**: Tailwind CSS [Source: docs/architecture/tech-stack.md]
    *   **State Management**: React Hooks (`useState`) for local state management within the `generator-view.tsx` (or equivalent) component. [Source: docs/architecture/frontend-architecture.md#state-management-architecture, docs/architecture/tech-stack.md]
    *   **Routing**: Next.js file-system-based App Router. Navigation from `/` to `/generate`. [Source: docs/architecture/frontend-architecture.md#routing-architecture]
    *   **Frontend Services Layer**: API calls will eventually be abstracted into a service layer using the browser's `fetch` API. [Source: docs/architecture/frontend-architecture.md#frontend-services-layer]

**Tasks / Subtasks:**
*   [x] Create the `/generate` page at `apps/web/src/app/generate/page.tsx`. (AC: 1)
*   [x] Implement navigation from the landing page CTA to `/generate`. (AC: 1)
*   [x] Add "Full Name" and "Location" input fields to the `/generate` page. (AC: 2)
*   [x] Create a `CertificatePreview` component at `apps/web/src/components/certificate-preview.tsx`. (AC: 3)
*   [x] Integrate the `CertificatePreview` component into the `/generate` page. (AC: 3)
*   [x] Implement real-time reflection of input field values in the `CertificatePreview` component. (AC: 5)
*   [x] Add a "Download PDF" button to the `/generate` page, initially non-functional. (AC: 6)
*   [x] Write unit tests for the `/generate` page to ensure input fields, `CertificatePreview`, and the "Download PDF" button are rendered. (AC: 2, 3, 6)
*   [x] Write unit tests for the `CertificatePreview` component to ensure it correctly displays passed data. (AC: 5)

**Dev Agent Record:**
*   **Agent Model Used:** Gemini
*   **Debug Log References:**
    *   Initial Jest configuration issues (SyntaxError, module resolution).
    *   Corrected `jest.config.js` `transform` and `moduleNameMapper` regex escaping.
    *   Added `import React from 'react';` to test files and `jest.setup.js` to resolve `ReferenceError`.
    *   Created missing `input.tsx` component.
*   **Completion Notes List:**
    *   Implemented `/generate` page with input fields and non-functional download button.
    *   Created `CertificatePreview` component for real-time updates.
    *   Integrated navigation from landing page to `/generate`.
    *   Wrote comprehensive unit tests for both `GeneratePage` and `CertificatePreview`.
    *   Resolved all Jest configuration and module resolution issues.
*   **File List:**
    *   `apps/web/src/app/generate/page.tsx` (created)
    *   `apps/web/src/components/certificate-preview.tsx` (created)
    *   `apps/web/src/app/page.tsx` (modified for navigation)
    *   `apps/web/src/app/generate/page.test.tsx` (created)
    *   `apps/web/src/components/certificate-preview.test.tsx` (created)
    *   `apps/web/components/ui/input.tsx` (created)
    *   `apps/web/jest.config.js` (modified for Jest configuration)
    *   `apps/web/jest.setup.js` (modified for Jest setup)
    *   `apps/web/tsconfig.json` (modified for path aliases)
*   **Change Log:**
    *   Implemented all acceptance criteria for Story 1.2.
    *   Resolved Jest testing environment setup issues.
    *   Fixed 404 error by removing duplicate `/app` directory.

**QA Results:**
