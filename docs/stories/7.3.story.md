
Status: Approved
---
**As a** user,
**I want** the frontend to be updated to use the new sequential `userId`,
**so that** I can create a user and see the new identifier correctly.
---
1. All frontend components that previously displayed or used `certificateNumber` now use the `userId`.
2. Client-side calls to the user creation server action are updated to point to the new `actions/user.ts` and its refactored functions.
3. The application correctly handles the `userId` returned from the backend.
4. The user creation flow is fully functional from the UI, and the new `userId` is correctly displayed or handled as required.
---
- [x] **Task 1: Identify and Update Frontend Components (AC: #1)**
    - [x] Search the `apps/web/src` directory for all instances of `certificateNumber`.
    - [x] In each identified component, replace the logic and UI elements to use the `userId` field instead.
- [x] **Task 2: Update Server Action Calls (AC: #2)**
    - [x] Locate the client-side code that calls the user creation server action.
    - [x] Modify the code to call the `createUser` function from `apps/web/src/lib/actions/user.ts`.
- [x] **Task 3: Handle Returned `userId` (AC: #3)**
    - [x] Ensure the frontend code correctly receives and processes the `userId` returned from the `createUser` action.
    - [x] Update any state management logic to store and use the new `userId`.
- [ ] **Task 4: End-to-End Testing (AC: #4)**
    - [ ] Manually test the entire user creation flow from the user interface.
    - [ ] Verify that a new user can be created successfully.
    - [ ] Confirm that the new `userId` is displayed correctly in the UI after creation.
---
### Dev Notes:
- The frontend code is located in `apps/web/src`.
- The user creation form is likely in a component under `apps/web/src/components`.
- The server action call will be in a client-side script, probably within a React component.

### Testing:
- Update existing frontend tests (Jest & React Testing Library) to reflect the change from `certificateNumber` to `userId`.
- Create new tests for any modified components to ensure they correctly handle the `userId`.
- Manually perform end-to-end testing of the user creation flow in the browser.
---
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| August 07, 2025 | 1.0 | Initial draft | Bob, SM |
