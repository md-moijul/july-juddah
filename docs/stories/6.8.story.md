---
Story: 6.8
Title: Refactor Component Tests & Remove __tests__ directory
Epic: 6
Status: To Do
---

### Story

As a developer, I want to refactor the remaining component tests to align with the new testing standard and remove the `__tests__` directory, so that the project structure is consistent and clean.

### Acceptance Criteria

- All tests for the components are co-located with their respective source files.
- All test files follow the `*.test.tsx` naming convention.
- All tests are structured using the "Arrange, Act, Assert" pattern.
- All tests are written from a user's perspective using React Testing Library.
- All mocks use `jest.fn()`.
- The `__tests__` directory is removed.

### Tasks

- [ ] Move and refactor `__tests__/CertificateDisplay.test.tsx`
- [ ] Move and refactor `__tests__/DisclaimerSection.test.tsx`
- [ ] Move and refactor `__tests__/FinalCtaSection.test.tsx`
- [ ] Move and refactor `__tests__/Footer.test.tsx`
- [ ] Move and refactor `__tests__/ImageBanner.test.tsx`
- [ ] Move and refactor `__tests__/PremiumItemsSection.test.tsx`
- [ ] Remove the `__tests__` directory.
