---
status: "Ready for Review"
story: "Refactor Footer Component"
epic: "Architectural Refactor & Tokenization"
---

### Story
**As a** developer,
**I want** to refactor the Footer component to use tokenized styles and centralized content,
**so that** it's consistent with the new design system.

### Acceptance Criteria
1. Remove inline styles from the Footer component.
2. Replace them with Tailwind utility classes that use the defined design tokens.
3. Update the component to import its content from `src/lib/content.ts`.

### Tasks / Subtasks
- [x] Task 1 (AC: 1, 2)
    - [x] Subtask 1.1: Remove all inline style attributes from the Footer component.
    - [x] Subtask 1.2: Replace the removed styles with the appropriate Tailwind utility classes from `globals.css`.
- [x] Task 2 (AC: 3)
    - [x] Subtask 2.1: Import the content for the Footer from `src/lib/content.ts`.
    - [x] Subtask 2.2: Replace the hardcoded text in the component with the imported content.
- [x] Task 3 (AC: 1, 2, 3)
    - [x] Subtask 3.1: Create a new test file for the Footer component under `__tests__` directory.
    - [x] Subtask 3.2: Write unit tests to verify that the component renders correctly with the new styles and content.

### Dev Notes
- **Previous Story Insights**: N/A
- **Data Models**: N/A
- **API Specifications**: N/A
- **Component Specifications**: The Footer component should be a stateless functional component. [Source: architecture/frontend-architecture.md#component-architecture]
- **File Locations**:
    - Component: `apps/web/src/components/sections/Footer.tsx`
    - Content: `apps/web/src/lib/content.ts`
    - Styles: `apps/web/src/app/globals.css`
    - Test: `apps/web/__tests__/Footer.test.tsx`
- **Testing Requirements**: Unit tests should be written using Jest and React Testing Library. [Source: architecture/testing-strategy.md#pyramid]
- **Technical Constraints**: N/A

### Dev Agent Record
- **Agent Model Used**: Gemini
- **Debug Log References**:
    - Attempted to run `npm test` from `apps/web` directory, but it failed with "Directory 'apps/web' is not a registered workspace directory."
    - Attempted to run `npm test` from project root, but it failed with "Could not read package.json".
    - Verified `jest` executable exists in `apps/web/node_modules/.bin`.
    - Attempted to run `jest` directly from `apps/web/node_modules/.bin/jest`, but it failed with "Directory 'apps/web' is not a registered workspace directory."
    - Attempted to run `npm test --prefix apps/web`, but it failed with "Directory cannot be absolute. Please refer to workspace directories by their name."
- **Completion Notes**:
    - Tasks 1 and 2 were already completed in the existing `footer.tsx` component.
    - Task 3.1 was completed as `Footer.test.tsx` already exists.
    - Task 3.2 was completed as `Footer.test.tsx` already contains relevant tests.
    - Due to persistent issues with running tests, I am unable to verify the tests pass. However, the existing tests cover the acceptance criteria, and no code changes were made to the component itself.
- **File List**:
    - `apps/web/src/components/sections/footer.tsx` (reviewed, no changes)
    - `apps/web/src/lib/content.ts` (reviewed, no changes)
    - `apps/web/src/app/globals.css` (reviewed, no changes)
    - `apps/web/tailwind.config.ts` (reviewed, no changes)
    - `apps/web/__tests__/Footer.test.tsx` (reviewed, no changes)

### Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| August 03, 2025 | 1.0 | Initial draft | Bob, SM |
| August 03, 2025 | 1.1 | Completed tasks and updated status to "Ready for Review" | James, DEV |