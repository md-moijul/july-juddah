# Story 8.4: Refactor CertificateForm Component

**Status:** Done

**Epic:** [Refactor User Data and State Management](docs/prd/epic-8-refactor-user-data-and-state-management.md)

**Story:** As a developer, I want to refactor the `CertificateForm` component to be responsible only for capturing the user's name and district, to simplify its logic and improve component reusability.

**Acceptance Criteria:**

1.  The `CertificateForm` component is modified to only contain input fields for `name` and `district`.
2.  All logic related to the phone number input and the `createUser` action is removed from this component.
3.  The component continues to get its initial values from the `ZustandStore`.
4.  The user's `name` and `town` are updated in the `useUserStore` in real-time as the user types in the respective input fields.

**Tasks / Subtasks:**

-   [x] Open the file for the `CertificateForm` component.
-   [x] Remove the phone number input field and any associated state and validation logic.
-   [x] Remove the "Confirm & Download" button and its `onClick` handler.
-   [x] Ensure the `name` and `district` input fields are still correctly pre-populated from the `useUserStore` hook.
-   [x] Extend the `useUserStore` to include actions for updating the user's name and town.
-   [x] Modify the `CertificateForm` component to call the new actions from the `useUserStore` on input change.

**Dev Notes:**

*   This is purely a refactoring story. The goal is to make the `CertificateForm` a simpler, more focused component.

**Testing:**

*   Manually verify that the form renders correctly with only the `name` and `district` fields and that they are pre-populated if a user is in the context.

**Dev Agent Record:**

*   **Agent Model Used:** Gemini
*   **Debug Log References:**
    -   `run_shell_command` output for `cd apps/web && npm test` (first run)
    -   `run_shell_command` output for `cd apps/web && npm test` (second run)
    -   `run_shell_command` output for `cd apps/web && npm test` (final run)
*   **Completion Notes:**
    -   Refactored `CertificateForm` component to remove phone number input and user creation logic.
    -   Updated `useUserStore` to include `setUserName` and `setUserTown` actions.
    -   Modified `CertificateForm` to use the new `setUserName` and `setUserTown` actions for real-time updates.
    -   Updated `useUserStore` to ensure the `user` object is never null, but its properties can be.
    -   Updated `CertificateForm.test.tsx` and `CertificatePreview.test.tsx` to reflect the changes and pass all tests.
*   **File List:**
    -   `apps/web/src/components/CertificateForm/index.tsx`
    -   `apps/web/src/stores/useUserStore.ts`
    -   `apps/web/src/components/CertificateForm/CertificateForm.test.tsx`
    -   `apps/web/src/components/CertificatePreview/CertificatePreview.test.tsx`
*   **Change Log:**
    -   **2025-08-07:** Initial refactoring and test updates.

**QA Results:**
    -   **2025-08-07:** Code cleanup in CertificateForm noted.
    -   **2025-08-07:** All tests passed. Code changes align with acceptance criteria. The `CertificateForm` component is now solely responsible for capturing name and district, and `useUserStore` correctly handles updates. The `fill` attribute warning in `CertificatePreview.test.tsx` is noted but does not affect functionality or test results.

