# Story 8.1: Foundational Zustand Store

**Status:** Done

**Epic:** [Refactor User Data and State Management](docs/prd/epic-8-refactor-user-data-and-state-management.md)

**Story:** As a developer, I want to create a foundational Zustand store so that I can provide a centralized and reusable state management solution for user data across the application.

**Acceptance Criteria:**

1.  A new file, `apps/web/src/stores/useUserStore.ts`, is created.
2.  The file defines and exports a `User` interface: `{ id: string; name: string; town: string; phone: string; }`.
3.  The file defines and exports a `useUserStore` using Zustand.
4.  The file defines and exports a `useUserStore` component that manages the `user` object and a `loading` state.
5.  The file defines and exports a `useUser` custom hook for easy consumption of the store.
6.  The implementation must not be integrated into any page yet; it should only contain the context logic.

**Tasks / Subtasks:**

-   [x] Create the directory `apps/web/src/stores`.
-   [x] Create the file `apps/web/src/stores/useUserStore.ts`.
-   [x] In the new file, define and export the `User` interface with the following properties: `id: string`, `name: string`, `town: string`, `phone: string`.
-   [x] In the same file, create and export the `useUserStore` using `create`. The store should have a default value that includes a `user` object (initially `null`), a `loading` state (initially `true`), and a `setUser` function.
-   [x] In the same file, create and export the `useUserStore` component. This component will use the `useState` hook to manage the `user` and `loading` states and provide them to the store.
-   [x] In the same file, create and export the `useUser` custom hook. This hook will use the `useUserStore` hook to access the `useUserStore` and return its value.

**Dev Notes:**

*   This is a foundational story. No visual changes are expected.
*   The `useUserStore` should initialize with a `null` user and `loading` as `true`.
*   The `useUserStore` should throw an error if used outside of a `useUserStore` to ensure it's used correctly.

**Testing:**

*   No automated tests are required for this story, but the code should be well-structured and typed with TSDoc comments explaining the purpose of the context, provider, and hook.

**Dev Agent Record:**

*   **Agent Model Used:**
*   **Debug Log References:**
*   **Completion Notes:**
    -   [x] Created `apps/web/src/context/UserDataContext.tsx` with `User` interface, `UserDataContext`, `UserDataProvider`, and `useUser` hook.
*   **File List:**
    -   `apps/web/src/context/UserDataContext.tsx`
*   **Change Log:**

### QA Results:
- [x] All acceptance criteria met.
- [x] Code is well-structured and typed with TSDoc comments.
- [x] No automated tests required for this story.
