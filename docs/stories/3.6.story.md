---
status: "Done"
story: "Refactor Final CTA Section Component"
epic: "Architectural Refactor & Tokenization"
---

### Goal
Refactor the `FinalCtaSection` component to utilize the new tokenized styles defined in `globals.css` and consume content from the centralized `content.ts` file.

### Acceptance Criteria
1.  The `FinalCtaSection` component uses CSS variables from `apps/web/src/app/globals.css` for its styling.
2.  All static text and image paths within the `FinalCtaSection` component are sourced from `apps/web/src/lib/content.ts`.
3.  The visual appearance and functionality of the `FinalCtaSection` component remain unchanged after refactoring.
4.  No other components are modified.
5.  The application still runs and displays correctly.

### Dev Notes

*   **Coding Standards:** Use shared types from `packages/shared`, follow the defined component structure, use the service layer for API calls, and use Tailwind CSS for all styling. PascalCase for components, camelCase for functions/variables. [Source: docs/architecture/coding-standards.md]
*   **Components:** The Frontend Application is responsible for rendering the UI, managing user input, displaying the live certificate preview, and orchestrating PDF generation. Technology Stack: Next.js, React, TypeScript, Tailwind CSS, shadcn/ui. [Source: docs/architecture/components.md]
*   **Frontend Architecture - Component Architecture:** The `src/components/sections` directory will contain a dedicated component for each major section defined in `content.json`. The target component is `apps/web/src/components/sections/final-cta-section.tsx`. [Source: docs/architecture/frontend-architecture.md]
*   **Frontend Architecture - State Management:** For V1.0, state is managed locally within components using `useState`. This component should not introduce global state. [Source: docs/architecture/frontend-architecture.md]
*   **Tech Stack:** `shadcn/ui` provides accessible, composable components. Tailwind CSS is used for styling. [Source: docs/architecture/tech-stack.md]
*   **Unified Project Structure:** The file path for the component is `apps/web/src/components/sections/final-cta-section.tsx`. [Source: docs/architecture/unified-project-structure.md]
*   **Testing Strategy:** Unit and component tests should be written using Jest and React Testing Library. Tests should be co-located with the code. [Source: docs/architecture/testing-strategy.md]

### Tasks / Subtasks

1.  **Analyze `FinalCtaSection` Component:**
    *   [x] Read `apps/web/src/components/sections/final-cta-section.tsx` to understand its current implementation, styling, and data usage.
    *   [x] Identify existing hardcoded styles (e.g., direct color values, font sizes) that need to be replaced with CSS variables from `globals.css`.
    *   [x] Identify any static text or image paths within the component that can be sourced from `apps/web/src/lib/content.ts`.
2.  **Refactor Styling:**
    *   [x] Modify `apps/web/src/components/sections/final-cta-section.tsx` to replace direct Tailwind CSS classes or inline styles with CSS variables (e.g., `var(--primary)`) defined in `apps/web/src/app/globals.css` where applicable. (AC: The `FinalCtaSection` component uses CSS variables from `apps/web/src/app/globals.css` for its styling.)
    *   [x] Ensure the component's visual appearance remains pixel-perfect consistent with the original design.
3.  **Integrate Centralized Content:**
    *   [x] Import the `content` object from `apps/web/src/lib/content.ts` into `apps/web/src/components/sections/final-cta-section.tsx`.
    *   [x] Replace any hardcoded text strings or image `src` attributes within the component with corresponding values accessed from the imported `content` object (e.g., `content.final_cta_section.headline`). (AC: All static text and image paths within the `FinalCtaSection` component are sourced from `apps/web/src/lib/content.ts`.)
4.  **Create/Update Tests:**
    *   [x] Create or update unit/component tests for `apps/web/src/components/sections/final-cta-section.tsx` using Jest and React Testing Library. (AC: The application still runs and displays correctly.)
    *   [x] Tests should verify that the component renders correctly with the new tokenized styles and centralized content.
    *   [x] Ensure that the refactoring has not introduced any regressions in functionality or appearance.
5.  **Verify Application Functionality:**
    *   [x] Run the application locally (e.g., `npm run dev`) and visually inspect the `FinalCtaSection` component to ensure it functions and displays correctly. (AC: The visual appearance and functionality of the `FinalCtaSection` component remain unchanged after refactoring; The application still runs and displays correctly.)
    *   [x] Confirm that no existing components other than `apps/web/src/components/sections/final-cta-section.tsx` were modified. (AC: No other components are modified.)

### Dev Agent Record
- **Agent Model Used:** Gemini
- **Debug Log References:**
- **Completion Notes:** Refactored FinalCtaSection component to use CSS variables from globals.css and content from content.ts. Updated existing test file for the component.

### File List
- apps/web/src/components/sections/final-cta-section.tsx
- apps/web/src/lib/content.ts
- apps/web/__tests__/FinalCtaSection.test.tsx

### Change Log

### QA Results

### Review Date: August 3, 2025

### Reviewed By: Quinn (Senior Developer QA)

### Code Quality Assessment

All tasks outlined in the story have been completed and verified.

### Refactoring Performed

Refactoring of `FinalCtaSection` component completed as per the story. Styles are now tokenized using CSS variables from `globals.css` and static content is sourced from `content.ts`.

### Compliance Check

- Coding Standards: [x] Assessed and compliant.
- Project Structure: [x] Assessed and compliant.
- Testing Strategy: [x] Assessed and compliant.
- All ACs Met: [✓] All Acceptance Criteria are met.

### Improvements Checklist

- [x] Replace any hardcoded text strings or image `src` attributes within the component with corresponding values accessed from the imported `content` object (e.g., `content.final_cta_section.headline`). (AC: All static text and image paths within the `FinalCtaSection` component are sourced from `apps/web/src/lib/content.ts`.)

### Security Review

Security review completed. No new security vulnerabilities introduced.

### Performance Considerations

Performance review completed. No significant performance impacts observed.

### Final Status

[✓ All tasks completed and verified]