
---
Status: Done
---
**As a** developer,
**I want** to update the database schema to remove the `certificateNumber` column from the `users` table,
**so that** the database aligns with the new user identification system.
---
1. The `certificateNumber` column is removed from the `users` table definition in `apps/web/src/db/schema.ts`.
2. A new database migration script is successfully generated using Drizzle Kit.
3. The migration script is reviewed and confirmed to correctly drop the `certificateNumber` column without affecting other data.
4. The migration is successfully applied to the development database.
---
- [x] **Task 0.1: remove captcha from everywhere  (AC: #1)**
- [x] **Task 1: Modify Drizzle Schema (AC: #1)**
    - [x] Open `apps/web/src/db/schema.ts`.
    - [x] Locate the `users` table definition.
    - [x] Remove the line defining the `certificateNumber` column.
- [x] **Task 2: Generate Database Migration (AC: #2)**
    - [x] Open a terminal in the `apps/web` directory.
    - [x] Run the command `npx drizzle-kit generate:pg` to create the migration script.
- [x] **Task 3: Verify Migration Script (AC: #3)**
    - [x] Locate the newly generated migration file in the `apps/web/drizzle` directory.
    - [x] Review the SQL to ensure it only contains the `ALTER TABLE "users" DROP COLUMN "certificateNumber"` statement.
- [x] **Task 4: Apply Migration (AC: #4)**
    - [x] Open a terminal in the `apps/web` directory.
    - [x] Run the command `npx drizzle-kit migrate:pg` to apply the migration to the development database.
---
### Completion Notes:
- Removed `certificateNumber` column from `apps/web/src/db/schema.ts`.
- Generated and applied new Drizzle migration.
- Verified migration script and applied to development database.

### Dev Notes:
- The `users` table schema is located at `apps/web/src/db/schema.ts`.
- The Drizzle Kit configuration is in `apps/web/drizzle.config.ts`.
- The database connection string is in `apps/web/.env.local`.

### Testing:
- No specific testing standards are required for this story as it is a database migration. The developer should manually verify the changes in the database.

### File List:
- apps/web/src/db/schema.ts
- apps/web/drizzle/0000_keen_lightspeed.sql

### File List:
- apps/web/src/db/schema.ts
- apps/web/drizzle/0000_keen_lightspeed.sql
---
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| August 07, 2025 | 1.0 | Initial draft | Bob, SM |
