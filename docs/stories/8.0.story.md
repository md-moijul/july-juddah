---
story:
  title: "Story 8.0: Migrate State Management from React Context to Zustand"
  epic: 8
  sprint: 1
  status: "Ready for Review"
  owner: "Winston, Architect"
  description: "As a developer, I want to replace the foundational UserDataContext with a more efficient and simpler Zustand store to manage our global user state."
  acceptanceCriteria:
    - "The `zustand` library is added as a project dependency."
    - "A new Zustand store is created (e.g., in `src/stores/useUserStore.ts`)."
    - "The store manages the user object and a loading state, replacing the functionality of the old `UserDataProvider`."
    - "The `useUser` custom hook is refactored to get its state from the Zustand store."
    - "The original `UserDataContext.tsx` file is deleted."
    - "The `GeneratePage` is refactored to remove the `UserDataProvider` and use the new `useUser` hook."
  tasks:
    - "[x] Install the `zustand` dependency. (AC: 1)"
    - "[x] Create the new store file at `src/stores/useUserStore.ts`. (AC: 2)"
    - "[x] Define the store's state and actions for setting the user and loading status. (AC: 3)"
    - "[x] Refactor the `useUser` hook to use the `useUserStore`. (AC: 4)"
    - "[x] Delete the `src/context/UserDataContext.tsx` file. (AC: 5)"
    - "[x] Update `apps/web/src/app/generate/page.tsx` to remove the context provider and use the new hook. (AC: 6)"
  devNotes:
    - "**Project Structure:** Create a new directory `src/stores` for the Zustand store. [Source: docs/architecture.md#unified-project-structure]"
    - "**State Management:** The architecture document currently specifies React Hooks for state management. This story updates that to Zustand. [Source: docs/architecture.md#tech-stack]"
    - "**Testing:** Ensure existing tests for the `GeneratePage` are updated to reflect the new state management approach. [Source: docs/architecture.md#testing-strategy]"
---